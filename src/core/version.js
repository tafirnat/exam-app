/**
 * One version number, in one place.
 *
 * The guide shows it and package.json declares it, and two copies of a version
 * string drift the first time someone bumps only one of them.
 * tests/user-guide.test.mjs keeps them equal.
 */
export const APP_VERSION = '1.1.0';
