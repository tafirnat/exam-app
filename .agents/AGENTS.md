# Workspace Rules for Exam App

- **Automatic GitHub Push**: For this project (`Exam App`), all UI, style, and code modifications must be automatically built, committed, and pushed to GitHub (`origin main`) immediately after completing the implementation. This is necessary because live testing relies on cloud deployments (GitHub Actions / Pages / Vercel).
